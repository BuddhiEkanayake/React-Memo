# Node modules (dependencies)
node_modules/

# Build output (Vite)
dist/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
pnpm-error.log*

# VS Code settings (if using VS Code)
.vscode/

# MacOS system files
.DS_Store

# IDE-specific files (WebStorm, JetBrains, etc.)
.idea/
*.iml

# Optional: Ignore package-lock.json or yarn.lock (if you want to avoid lockfile updates)
# package-lock.json
# yarn.lock
